nav(class='navbar navbar-default navbar-fixed-top')
	div.container-fluid
		div.navbar-header
			a(href='/')
				img(src='/images/chart.png' height='50px' width='50px')
		div(id='navbar' class='navbar-collapse collapse')
			ul(class='nav navbar-nav')
				li
					a(href='/') Home
				li
					a(href='/market') Market
				li
					a(class='dropdown-toggle' role='button' data-toggle='dropdown' aria-haspopup='true' aria-expanded='false') Portfolio
						span.caret
					ul(class='dropdown-menu')
						if ids
							if ids.length === 0
								li
									a(href='/portfolio') Create a portfolio now!
							else
								li
									a(href='/portfolio') Portfolio Overview
								li(role='separator' class='divider')
								each val in ids
									li
										a(href='/portfolio/' + val) Portfolio #{val}
						else
							li
								a(href='/login?redirectTo=portfolio') Login to see portfolios
				li
					a(href='#') Insights
				li
					a(href='/testing') Testing &nbsp
						span(class='glyphicon glyphicon-warning-sign')
			ul(class='nav navbar-nav navbar-right')
				if isLoggedIn === false
					li
						a(href='/login') Login
				else
					li
						a(href='/logout') Logout